/* 
const metaSchema = new Schema({
    votes:Number,
    favs:Numbers
})

constt commentsSchema =new Schema({
    body:String,
    date : {type:date , default:Date.now},
})

const blogSchema = new schema({
    title :String,
    author:String,
    body:String,
    comments : [comm]
}) */