const localmod = require('./localmod') // here we requested  module(coremod) and we will 
//store in one variable it has access the coremod(module we requested) 
localmod.name() //when we call coremod.name it goes to coremod file that is called and excutes the code 
localmod.detail();